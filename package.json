{
  "name": "postgres-range",
  "type": "module",
  "version": "1.1.4",
  "description": "Range data type parser and serializer for PostgreSQL",
  "main": "index.js",
  "repository": "https://github.com/martianboy/postgres-range",
  "author": "Abbas Mashayekh <martianboy2005@gmail.com>",
  "license": "MIT",
  "scripts": {
    "test": "vitest",
    "build": "rimraf dist && tsc -p .",
    "dev": "rimraf dist && tsc -p . --watch --sourcemap --declarationMap"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}
